{
  "name": "window-button-layout",
  "version": "0.1.1",
  "description": "RetrieveRetrieve OS window buttons layout (close, maximize, minimize...)",
  "keywords": ["window", "button layout"],
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --outDir dist -d",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@types/node": "^12.12.7",
    "typescript": "^3.7.2"
  }
}